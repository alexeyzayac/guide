### 1. Базовые пакеты для сборки модулей ядра (обязательно)
Установка:
````bash
apt install -y \
  dkms \
  build-essential \
  linux-headers-$(uname -r) \
  libvirt-dev \
  libvirt-daemon-system \
  libvirt-clients \
  pkg-config \
  ruby-dev \
  gcc make
````
Проверка:
````bash
ls /lib/modules/$(uname -r)/build
````
---
### 2. Установка Vagrant (HashiCorp repo)
Ключ HashiCorp:
````bash
wget -qO- https://apt.releases.hashicorp.com/gpg \
  | gpg --dearmor \
  | tee /usr/share/keyrings/hashicorp.gpg >/dev/null
````
Добавление репозитория:
````bash
echo "deb [arch=amd64 signed-by=/usr/share/keyrings/hashicorp.gpg] \
https://apt.releases.hashicorp.com trixie main" \
| tee /etc/apt/sources.list.d/hashicorp.list
````
Установка:
````bash
apt update
apt install -y vagrant
vagrant --version
````
---
### 3. Плагины Vagrant (обязательно)
##### Команды с плагинами
````bash
vagrant plugin list #Список установленных плагинов
vagrant plugin install <имя_плагина> #Установка плагинов
vagrant plugin uninstall <имя_плагина> #Удаление плагинов
vagrant plugin update <имя_плагина> #Обновление плагинов
vagrant plugin repair #Пересчитывает зависимости, переустанавливает отсутствующие или несовместимые версии
````
##### Лучшие плагины 
````bash
vagrant plugin install vagrant-vbguest #Синхронизация Guest Additions
vagrant plugin install vagrant-reload #Перезагрузка без проблем
vagrant plugin install vagrant-libvirt # Позволяет работать с KVM / libvirt
````
##### Рекомендация, это лечит 90% странных конфликтов Ruby-зависимостей
````bash
vagrant plugin expunge --reinstall
````
##### Список плагинов
| Категория                | Плагин                   | Назначение                                                    |
| ------------------------ | ------------------------ | ------------------------------------------------------------- |
| **Универсальные**        | `vagrant-env`            | Загрузка переменных из `.env`                                 |
|                          | `vagrant-scp`            | Копирование файлов через SCP                                  |
|                          | `vagrant-timezone`       | Установка часового пояса гостя                                |
|                          | `vagrant-hostmanager`    | Управление `/etc/hosts` на хосте и гостях                     |
|                          | `vagrant-hostsupdater`   | Автообновление `/etc/hosts` на хосте                          |
|                          | `vagrant-proxyconf`      | Настройка HTTP/HTTPS прокси для ВМ                            |
|                          | `vagrant-triggers`       | Выполнение скриптов до/после команд Vagrant                   |
|                          | `vagrant-cachier`        | Кэширование пакетов (apt, yum, gem, npm)                      |
|                          | `vagrant-reload`         | ✔✔✔ Перезагрузка ВМ без ошибок                                |
| **VirtualBox**           | `vagrant-vbguest`        | ✔✔✔ Автообновление Guest Additions                            |
|                          | `vagrant-disksize`       | Задание размера диска                                         |
|                          | `vagrant-winrm`          | Управление Windows-гостями через WinRM                        |
| **KVM / libvirt**        | `vagrant-libvirt`        | ✔✔✔ Провайдер для KVM                                         |
|                          | `vagrant-mutate`         | Конвертация коробок между провайдерами (VirtualBox ↔ libvirt) |
|                          | `vagrant-sshfs`          | Монтирование папок через SSHFS                                |
|                          | `vagrant-nfs_guest`      | Настройка NFS внутри гостевой ВМ                              |
| **Proxmox**              | `vagrant-proxmox`        | Управление ВМ и LXC контейнерами в Proxmox VE                 |
|                          | `vagrant-pve`            | Альтернативный провайдер для Proxmox                          |
| **Облачные**             | `vagrant-aws`            | Amazon Web Services                                           |
|                          | `vagrant-google`         | Google Cloud Platform                                         |
|                          | `vagrant-vsphere`        | VMware vSphere                                                |
| **Утилиты / Разработка** | `vagrant-exec`           | Выполнение команд на хосте из контекста Vagrant               |
|                          | `vagrant-rsync-back`     | Синхронизация файлов из гостя обратно на хост                 |
|                          | `vagrant-snap`           | Управление снимками (snapshots)                               |
|                          | `vagrant-omnibus`        | Автоустановка Chef на гостевой ВМ                             |
|                          | `vagrant-berkshelf`      | Интеграция с Berkshelf (Chef)                                 |
| **Работа с коробками**   | `vagrant-box-update`     | Автоматическая проверка обновлений коробок                    |
|                          | `vagrant-packer`         | Интеграция с Packer (сборка своих коробок)                    |
| **Экспериментальные**    | `vagrant-docker-compose` | Управление docker-compose из Vagrant                          |
|                          | `vagrant-kubernetes`     | Развёртывание мини-кластера Kubernetes                        |

---


